# Разведка Дзен — dzen.ru/id/69a6a56804c3ba5d0aadf101 (канал Alex'а)

Дата: 2026-07-23. Канал «Александр Петров», 45 подписчиков, логин Дзена `gmdidro@gmail.com`.

## Публичная страница — работает
- URL: `https://dzen.ru/id/69a6a56804c3ba5d0aadf101`.
- `publisherId` = `69a6a56804c3ba5d0aadf101` (в HTML `"publisherId":"..."`).
- Отдаёт: **число подписчиков** (45) и **попостовые просмотры** (иконка глаза у каждой карточки: 1, 2, 2, 7, 7…), лайки, комментарии. Лента материалов + вкладка «Статьи».

## Студия — НЕДОСТУПНА этому каналу ⛔
- `https://dzen.ru/profile/editor` → **404 «Страница не найдена»**.
- `https://dzen.ru/profile/editor/id/<id>/publications-stat` → редирект на публичную страницу.
- `csrfToken` в HTML **не находится** нигде; ссылок на editor/stat/studio на публичной странице **нет**.
- Причина: у маленького канала без монетизации нет доступа в Дзен-Студию. У клиентского `osnova_capital` (5000+ подписчиков, монетизация) Студия есть — именно её использует скрипт Маши `dzen_snapshot.py`.

## Вывод
**XLSX-поток из плана (Task 4) на кабинете Alex'а протестировать НЕЛЬЗЯ** — нет Студии, нет csrf, нет endpoint `publisher-publications-rich-stat-xls`. Код Task 4 остаётся корректным для продакшна (клиентский канал), но на тесте Alex'а его нечем прогнать.
Единственное, что даёт тестовый канал: подписчики + попостовые просмотры с **публичной** страницы (деградированный набор, парсинг DOM/скриншота).
